# WorldScene.gd
extends Node2D
class_name WorldScene

# Base class for all world scenes (Hostel, Compound, ClassroomBlock).
# TODO: Implement common setup, NPC spawning, time lighting, exits.

func _ready() -> void:
	print("[WorldScene] Initialized (placeholder)")